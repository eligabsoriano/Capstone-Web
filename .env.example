# Backend API URL
# Use https://localhost:8443 when running with mTLS enabled
VITE_API_URL=http://localhost:8000

# Certificate Pinning — Server SPKI SHA-256 hash
# Get this value from: python scripts/generate_certs.py (in Capstone_Backend)
# Format: sha256/BASE64_HASH (supports multiple comma-separated pins for rotation)
VITE_SERVER_CERT_PIN=
